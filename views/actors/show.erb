<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= name %></title>
  </head>

  <body>
    <h2><%= name %></h2>

    <table>
       <tr>

         <th>title</th>
         <th>character</th>
       </tr>
        <% roles.each do |row| %>
       <tr>
         <td><a href='/movies/<%= row['title'] %>'><%= row['title'] %></a></td>

         <td><%= row['character']%></td>

       </tr>
      <% end %>
    </table>

  </body>
</html>
